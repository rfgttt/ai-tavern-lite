# AI Tavern 2.0 Preview

版本：`2.1.0-preview.1`

## 目标

2.0 Preview 将 AI Tavern 从“为少数卡片写固定面板的聊天客户端”升级为“安全、可回滚、可扩展的角色卡运行时”。重点不是直接执行角色卡携带的前端代码，而是保留其数据与显示意图，再转换成平台原生组件。

## 会话工作区

- ChatWorkspace 始终挂载。
- 设置和记忆页面作为覆盖层显示。
- 消息、状态、时间线、草稿和滚动位置按会话缓存。
- 返回聊天时立即使用缓存，随后静默刷新服务器数据。
- 点击当前会话不会清空消息。
- 加载失败会显示错误和重试入口。
- 历史消息不会在每次返回时重新播放入场动画。

## 角色卡兼容运行时

导入的 Character Card V2/V3 会生成兼容性报告，识别：

- 世界书和备用开场
- Regex 脚本数量
- MVU / JSONPatch 变量协议
- 骰子、战斗检定和战斗快照
- 状态面板与声明式 UI Manifest
- 角色资产与群聊开场
- 未识别扩展字段

未知扩展会保留，不会因为当前无法理解而在保存时丢失。任意 JavaScript 默认不执行。

## 消息 AST 与多角色对白

后端在回复完成时生成结构化消息片段：

- `dialogue`：具名对白
- `narration`：旁白
- `thought`：心理活动
- `action`：动作
- `markdown`：普通内容

说话人颜色以名字稳定哈希生成；同一个角色在整个会话中保持相同颜色。卡片未来可通过安全 Manifest 覆盖颜色和头像。

## 通用动态数据

运行时保留标准字段和 `custom` 字段。平台不认识的数字、字符串、数组和对象会自动进入“卡片动态数据”面板。每条 AI 回复还会附带该轮状态补丁，用户可以查看：

- 更新操作
- 变量路径
- 新值
- 被拒绝更新及原因

## 声明式界面

支持 `ai-tavern-ui/1` Manifest 的安全组件：

- `text`
- `tag`
- `progress`
- `key-value`
- `stat-grid`

卡片只能选择组件和绑定数据路径，不能注入 React 代码，也不能访问 API Key、数据库或主页面 DOM。

## Prompt 规划

上下文按平台规则、角色设定、运行时、世界书、记忆、后历史指令和最近聊天分配预算。世界书不会再无限占用上下文，最近连续聊天始终拥有独立预算。诊断包会继续记录纳入的世界书数量、历史裁剪数量和 Token 估算。

## Persona、群聊与分支

- Persona：玩家名称、代称和描述，可设置默认身份。
- 群聊：多个角色组成编组，新会话会把出演表注入 Prompt，并要求明确标注发言者。
- 剧情分支：保存消息与运行时快照，之后可恢复该节点。

## 安全边界

- 不直接执行卡片 JavaScript。
- 不允许卡片读取密钥或完整数据库。
- 不把未清洗 HTML 插入主页面。
- 状态写入继续经过路径、类型、大小和操作数量限制。
- 诊断日志默认不记录完整聊天、Prompt 或 API Key。

## Preview 限制

- 旧 SillyTavern/Risu 的任意 JavaScript 插件不会原样运行。
- Regex 仍以安全子集为主，不保证所有复杂替换脚本等价。
- 群聊当前使用统一模型生成多角色回复，不是多模型并发代理。
- 声明式 UI Manifest 是 AI Tavern 自有的预览协议，后续可能迭代字段。
