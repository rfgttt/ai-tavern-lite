# Tavern MVU State Recovery 2.4.3 R2

R2 修复真实模型忽略角色卡 `<UpdateVariable>` 协议时，运行时状态静默不变化的问题。

## 两阶段状态更新

1. 主模型正常生成玩家可见的角色扮演正文。
2. 本地解析主回复中的角色卡变量命令。
3. 主回复已有有效状态操作时，直接应用，不产生第二次请求。
4. 角色卡声明变量协议、主回复完整结束且没有有效操作时，执行一次受限状态提取请求。
5. 提取结果经过路径白名单、类型检查、数值限幅和状态引擎验证后才写入。

补救请求只包含：

- 本轮用户消息；
- 本轮 AI 可见正文；
- 当前允许更新的角色卡字段及其值；
- 本轮已注入的变量规则。

补救请求不包含完整聊天历史、API Key、自定义认证头的值、角色卡可执行脚本或无关世界书内容。

## 安全约束

- 每个主回复最多触发一次补救请求；
- 只允许修改已经存在于角色卡状态中的路径；
- 只接受 `increment`、`replace`、`append`、`remove_value`；
- 好感、恐惧、依赖、信任、亲密等关系数值的补救增量每轮限制在 `-2..2`；
- 单轮最多 16 个提取操作；
- 非法 JSON、未知字段、类型不匹配或过长数据不会写入状态；
- 用户停止、主请求失败、角色卡未声明变量协议时不触发补救；
- 补救失败不影响已经生成的正文。

## 版本和诊断语义

空 patch 不再被标记为已应用，也不会无意义增加运行时版本。

诊断状态新增：

- `state_update_source`: `primary_response`、`fallback_extractor` 或 `none`；
- `fallback_attempted`；
- `fallback_reason`；
- `fallback_response_characters`；
- `state_changed`；
- 实际应用、拒绝和解析错误数量。

设置页提供“自动补全角色状态更新”开关，默认开启。关闭后不会产生补救模型请求。

## 数据与兼容性

- 不需要数据库迁移；
- R1 已创建的会话和状态继续使用；
- 原始角色卡脚本仍不执行；
- 主回复已经正确输出 MVU 命令时，行为与 R1 相同。

## R2 Hotfix 1

真实 OpenAI 兼容接口测试发现，R2 的补救调用复用了主剧情流 Provider，并以流式增量拼接结构化 JSON；部分接口会在该路径返回空内容或在请求阶段失败。Hotfix 1 改为独立 Provider 实例、`stream=false` 完整响应，并增加细分诊断。参见 `TAVERN_MVU_RECOVERY_2.4.3_R2_HOTFIX1.md`。


## Hotfix 2 补充

官方 DeepSeek V4 的状态补救请求会使用非思考模式和 1024 token 输出上限；其他兼容 Provider 保持标准非流式行为。详见 `TAVERN_MVU_RECOVERY_2.4.3_R2_HOTFIX2.md`。
