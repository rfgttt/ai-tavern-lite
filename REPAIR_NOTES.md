# 修复说明（2026-07-14）

## 已修复

- 聊天 Prompt 不再重复加入当前用户消息。
- SSE 增加 `message`、`done`、`error` 结构化事件，前端使用服务端消息 ID 和最终状态。
- 停止生成不再被覆盖为 `complete`，并修复“首个字符到达前停止”会丢失取消信号的问题。
- 重新生成改为调用 `/api/chat/regenerate`，数据库和界面不再产生两份助手回复。
- 用户消息立即显示；助手消息会正确进入 `complete`、`stopped` 或 `error` 状态。
- 删除了每次发送时创建且不关闭的无效 `EventSource`。
- 设置中的服务商名称和用户名可以持久化；API Key 可以明确清除。
- 生成参数增加范围及上下文预算验证。
- API Key 和自定义请求头中的密钥值会从流式错误与日志中脱敏。
- SQLite 默认路径固定为 `backend/data/ai_tavern.db` 的绝对路径，不再依赖启动目录。
- 新增消息序号唯一约束；旧数据库启动时会重新编号并补唯一索引。
- 删除会话时同时删除会话级记忆；Memory 新模型带会话外键关系。
- 会话在消息新增、编辑、删除、生成完成、停止或失败时更新活跃时间。
- Prompt 历史不再为了“至少 8 条”而强行突破上下文预算。
- 超大上传会在读到限制值加 1 字节时立即拒绝。
- 安装脚本改用 `npm ci`，前端构建或测试失败时会中止，而不是继续显示安装成功。
- Vite 升级到 8.1.4，`npm audit` 为 0 漏洞。
- 发布忽略规则覆盖数据库、备份、缓存、临时文件、node_modules 和构建产物。

## 沉浸式运行时扩展

- 新增会话级场景、角色、关系、冒险、任务、背包与战斗状态。
- 新增结构化隐藏状态协议和受约束状态补丁引擎。
- 新增回合快照、事件时间线、剧情回滚与状态安全重新生成。
- 新增本轮世界书触发、动态行动选项、骰子和战斗检定原生组件。
- 新增 D&D 冒险者创建器和只读战斗地图快照。
- 兼容 MVU `UpdateVariable/JSONPatch` 文本协议，但不执行角色卡脚本或 HTML。
- 删除最新状态回复会恢复状态；删除较早状态回复会要求使用剧情回滚。

## 验证结果

- 后端：`pytest -q`，测试全部通过。
- 前端：`npm run build` 成功。
- 依赖：`npm audit` 为 0 漏洞。

## 使用提醒

原始压缩包中包含过非空 API Key、聊天记录和数据库备份。即使修复包已经移除这些内容，也应立即在模型服务商后台吊销并重新生成原密钥。

如需保留原数据，可在轮换 API Key 后，将原来的 `backend/data/` 单独复制到修复版项目中。首次启动会执行兼容性迁移；操作前仍建议自行备份数据库。

## 尚未扩展的产品功能

本次聚焦正确性与安全，没有补做角色编辑/导出、会话重命名/导出、世界书编辑等尚未接入界面的功能，也没有引入完整的前端单元测试框架。
