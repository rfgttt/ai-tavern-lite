# P0 / P1 用户体验修复记录

## P0
- 编辑消息时由后端重新解析并持久化 segments、speaker metadata 与 render version，避免正文和结构化显示不一致。
- 禁止直接编辑或删除已有后续剧情的历史消息，返回 409 并提示使用分支或回滚。
- 重生成改为保留旧回复直到新回复完整成功；失败或停止时删除临时回复并保留旧版本。
- 关闭 Mock 后，真实模型配置不完整会明确报错，不再静默使用 Mock。
- 新建会话为开场问候保存初始状态快照，历史状态栏不再回退到最新状态。

## P1
- 角色和会话加载加入请求代次校验，避免快速切换时旧响应串台。
- 旧会话请求不能再覆盖新会话的 loading 和错误状态。
- 中文输入法组合态 Enter 不触发发送。
- 输入草稿仅在服务器确认用户消息后清空；早期失败保留草稿。
- 生成期间允许继续编辑下一轮草稿，文本框自动增高。
- 移动端选择或新建会话后自动收起侧栏。
- 测试连接使用临时表单数据，不再隐式保存设置。
- 新建记忆默认当前会话或当前角色，并提供显式作用域选择与徽标。
- 记忆搜索增加 250ms 防抖，空白记忆由前后端共同拒绝。

## 验证
- 后端测试：132 passed（76 + 56 分组运行）
- P0/P1 新增回归测试：7 passed
- 前端：`npm run build` 成功
