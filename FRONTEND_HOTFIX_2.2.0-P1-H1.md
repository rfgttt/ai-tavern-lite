# AI Tavern 2.2 Preview.1 Frontend Hotfix 1

修复：

- 会话加载时的平滑滚动与用户滚动发生竞态，导致保存的滚动位置被重新覆盖为列表底部。
- 首次加载会话时等待消息和运行时状态完成后，再一次性恢复滚动位置。
- 已保存的 `0` 顶部位置现在可以正确恢复，不再被当作“没有缓存”。
- 只有后续新增/流式消息在用户位于底部附近时才使用平滑跟随。
- 通用动态数据节点增加 `aria-expanded` 状态。
- 一键自检不再把已经展开的嵌套变量误点击为折叠后再检查。

本补丁不修改数据库、聊天记录、角色卡、API Key 或后端运行时数据。
