# 沉浸式角色卡运行时

AI Tavern Lite 现在把角色卡当作“持续世界”运行，而不只是把角色描述拼进 Prompt。

## 核心闭环

每个会话都拥有独立的运行状态：

- 当前场景：地点、时间、天气、氛围与目标
- 角色状态：情绪、表情、精力与当前目标
- 关系状态：好感、信任、张力、关系阶段与变化原因
- 冒险状态：角色等级、HP、AC、状态、资源、任务与背包
- 战斗状态：单位、先攻、位置、生命值、状态效果与事件
- 自定义状态：用于兼容角色卡的 MVU / JSONPatch 数据

模型回复可以携带隐藏的结构化数据。正文正常流式显示，隐藏数据在回复完成后校验、保存并渲染为原生组件。

## 原生输出协议

模型可在正文末尾输出：

```xml
<tavern_state>
{
  "patch": [
    {"op": "increment", "path": "/relationship/trust", "value": 2},
    {"op": "replace", "path": "/scene/location", "value": "地牢北侧通道"}
  ],
  "events": ["发现了一扇刻有符文的石门"],
  "choices": ["检查符文", "让队伍原地警戒"],
  "expression": "警觉"
}
</tavern_state>
```

玩家不会看到该数据块。后端会限制操作数量、路径深度、状态大小和关键数值范围；生成中止或失败时不会应用半截状态。

## 兼容协议

运行时识别并转换以下文本协议：

- `<UpdateVariable><JSONPatch>...</JSONPatch></UpdateVariable>`
- `<dice>...</dice>`
- `<battlecheck>...</battlecheck>`
- `<battle>...</battle>`

这些内容会被转换成 AI Tavern 原生的状态变化、骰子卡片、战斗检定卡片和只读战斗快照。

## 安全边界

- 不执行角色卡携带的 JavaScript。
- 不直接注入角色卡携带的 HTML 或 CSS。
- Regex Script 只用于识别能力和文本协议，不执行替换脚本。
- 外部图片、Token 库和脚本不会自动加载。
- Markdown 继续经过 `rehype-sanitize` 安全渲染。

## 回滚与重新生成

每条完成的助手回复保存：

- 回复前状态
- 已应用和被拒绝的状态补丁
- 回复后状态
- 事件和行动选项
- 骰子、战斗检定与战斗快照
- 本轮触发的世界书条目

“重新生成”会先恢复旧回复之前的状态；“回到此处”会删除目标之后的消息并恢复对应状态。删除最新的状态回复同样会恢复回复前状态，删除较早回复则会要求使用回滚功能。

## D&D / 冒险卡

冒险模式提供：

- 原生角色创建与编辑器
- 等级、种族、职业、背景、阵营、HP 与 AC
- D&D 风格 `角色列表` / `当前角色ID` 自定义状态
- 骰子与战斗检定结果
- 单位位置、先攻、生命值和状态效果快照
- 任务、世界书触发和剧情足迹

完整的可拖拽地图编辑器、规则书自动裁决和多人联机不在当前版本范围内。
